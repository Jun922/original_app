<%= render "shared/header" %>

<div class="row">
  <section class="stats">
    <%= render 'users/stats' %>
    <% if @users.any? %>
      <div class="user_avatars">
        <% @users.each do |users| %>
          <%= link_to @user.name, user_path(@user.id) %>
        <% end %>
      </div>
    <% end %>
  </section>
</div>
