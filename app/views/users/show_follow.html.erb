<%= render "shared/header" %>

<div class="row">
  <section class="stats">
    <%= render 'users/stats' %>
    <% if @users.any? %>
      <div class="user_avatars">
        <% @users.each do |user| %>
          <%= link_to @users.name, user_path(@users.ids) %>
        <% end %>
      </div>
    <% end %>
  </section>
</div>
